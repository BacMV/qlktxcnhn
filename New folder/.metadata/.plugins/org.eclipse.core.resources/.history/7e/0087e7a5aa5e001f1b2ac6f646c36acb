package com.mycompany.qlktx;

import javax.swing.SwingUtilities;
import javax.swing.JFrame;
import view.DangKy_DangNhap_324;

public class QLKTX {
    public static void main(String[] args) {
        // Ensure that the GUI is created on the Event Dispatch Thread
      /*  SwingUtilities.invokeLater(new Runnable() {
            public void run() {
                try {
                    // Create and display the registration/login window
                    DangKy_DangNhap_324 frame = new DangKy_DangNhap_324();
                    frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Ensure the application exits when the window is closed
                    frame.setVisible(true);
                } catch (Exception e) {
                    // Handle and log exceptions
                    e.printStackTrace();
                    System.err.println("An error occurred while initializing the application.");
                }
            }
        });*/
      javax.swing.SwingUtilities.invokeLater(() -> {
            DangKy_DangNhap_324 login = new DangKy_DangNhap_324();
            // Khởi tạo ứng dụng
        });
    }
}
